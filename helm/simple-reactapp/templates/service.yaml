apiVersion: v1
kind: Service
metadata:
  name: {{ include "simple-reactapp.fullname" . }}
  labels:
    {{- include "simple-reactapp.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "simple-reactapp.selectorLabels" . | nindent 4 }}
